void setup() {
  size(800, 600);
  pos = new PVector (width/2, height/2);
  vel = new PVector (4, 3);
}
   void AgregarFuerza(PVector F){
   acel.add(F);
}
 void mover(){
   vel.add(Acel);
   acel.mult(0) //new PVector(0,0);
    pos.add(vel);
    
   contener();
 }
    void draw(){
   m.AgregarFuerza(G);
   m.mover();
   m.mostrar();
 }
